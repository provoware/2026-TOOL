name: Grundstruktur prüfen

on:
  push:
  pull_request:

jobs:
  repository-structure:
    runs-on: ubuntu-latest
    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Grundstruktur validieren
        run: ./scripts/validate_repo.sh
